import { prisma } from "../../../shared/lib/prisma.js";
export async function sessionServiceRead(token) {
    // const hasSession = await prisma.userSession.findUnique({ where: { }})
}
//# sourceMappingURL=session.service.js.map